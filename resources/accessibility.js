(()=>{const root=document.querySelector('[data-accessibility-menu]');if(!root)return;const html=document.documentElement,toggle=root.querySelector('[data-accessibility-toggle]'),panel=root.querySelector('[data-accessibility-panel]'),closeButton=root.querySelector('[data-accessibility-close]'),range=root.querySelector('[data-text-range]'),output=root.querySelector('[data-text-scale]'),decrease=root.querySelector('[data-text-decrease]'),increase=root.querySelector('[data-text-increase]'),reset=root.querySelector('[data-text-reset]'),key='eduvixo.accessibility.textScale',clamp=value=>Math.min(200,Math.max(100,Math.round(Number(value)/10)*10));let value=clamp(html.dataset.textScale||100),lastFocus=null;const focusable=()=>[...panel.querySelectorAll('button,input')].filter(item=>!item.disabled),apply=next=>{value=clamp(next);html.style.setProperty('--eduvixo-text-zoom',String(value/100));html.dataset.textScale=String(value);range.value=String(value);output.value=value+'%';output.textContent=value+'%';decrease.disabled=value<=100;increase.disabled=value>=200;try{localStorage.setItem(key,String(value))}catch{}},open=state=>{panel.hidden=!state;toggle.setAttribute('aria-expanded',String(state));if(state){lastFocus=document.activeElement;requestAnimationFrame(()=>closeButton.focus())}else if(lastFocus){lastFocus.focus();lastFocus=null}};toggle.addEventListener('click',()=>open(panel.hidden));closeButton.addEventListener('click',()=>open(false));range.addEventListener('input',()=>apply(range.value));decrease.addEventListener('click',()=>apply(value-10));increase.addEventListener('click',()=>apply(value+10));reset.addEventListener('click',()=>apply(100));document.addEventListener('click',event=>{if(!panel.hidden&&!root.contains(event.target))open(false)});document.addEventListener('keydown',event=>{if(panel.hidden)return;if(event.key==='Escape'){event.preventDefault();open(false)}else if(event.key==='Tab'){const items=focusable(),first=items[0],last=items.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}}});apply(value)})();
