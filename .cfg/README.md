# Local configuration

This directory is reserved for workstation-specific deployment and service configuration. All files are ignored by Git except this notice.

Never commit credentials, passwords, license keys, private keys, database connection strings or production host details. Keep the working copies only on trusted systems and rotate any value that may have been disclosed.
