<?php
declare(strict_types=1);
if(PHP_SAPI!=='cli')exit;
$root='/var/www/clients/client9/web121/web';foreach(array_keys(getenv())as$key)if(str_starts_with($key,'CMS_'))putenv($key);$config=(static fn(string$path):array=>require$path)($root.'/config/app.php');$db=new PDO('mysql:unix_socket=/run/mysqld/mysqld.sock;dbname='.$config['database']['name'].';charset=utf8mb4','root','',[PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION]);$counts=['unprocessed_calendar_events'=>(int)$db->query("SELECT COUNT(*) FROM notification_events WHERE source='calendar' AND processed_at IS NULL")->fetchColumn(),'pending_calendar_deliveries'=>(int)$db->query("SELECT COUNT(*) FROM notification_deliveries WHERE status='pending' AND JSON_UNQUOTE(JSON_EXTRACT(payload,'$.source'))='calendar'")->fetchColumn(),'due_calendar_reminders'=>(int)$db->query("SELECT COUNT(*) FROM calendar_reminders r INNER JOIN calendar_events e ON e.id=r.event_id WHERE r.status='pending' AND r.scheduled_at<=UTC_TIMESTAMP() AND e.status<>'cancelled' AND e.end_at>UTC_TIMESTAMP()")->fetchColumn()];echo json_encode($counts,JSON_THROW_ON_ERROR).PHP_EOL;
